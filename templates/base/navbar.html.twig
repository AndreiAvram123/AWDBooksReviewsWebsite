<nav class="navbar navbar-expand-lg navbar-light bg-light">

    <div class="container-fluid">
        <div class="collapse navbar-collapse" id="navbarSupportedContent">

            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="{{ url('home') }}">Home</a>
                </li>
                {% if is_granted('IS_AUTHENTICATED_FULLY') == true %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ url('create_book_review') }}">Create</a>
                    </li>
                {% endif %}

                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ url('admin') }}">Dashboard</a>
                    </li>
                {% endif %}

                {% if is_granted('ROLE_MODERATOR')%}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ url('moderator') }}">Content management</a>
                    </li>
                {%  endif %}
            </ul>
            {% if is_granted('IS_AUTHENTICATED_FULLY') == false %}
                {% if app.request.uri != url('login_path') %}
                    <div class="form-inline my-2 my-lg-0">
                        <button class="btn btn-primary my-2 my-sm-0" type="button" onclick="window.location = '{{ url('login_path') }}'">Log in</button>
                    </div>
                {% endif %}
            {% endif %}
           {% if is_granted('IS_AUTHENTICATED_FULLY') %}
               {{ include('components/user_nav_item.html.twig') }}
            {% endif %}


        </div>
    </div>
</nav>
